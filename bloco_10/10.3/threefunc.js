const upperCase = (string) => string.toUpperCase();

const firstCaracter = (string) => string[0];

const concatString = (string1, string2) => string1+string2;


module.exports = { upperCase, firstCaracter, concatString };